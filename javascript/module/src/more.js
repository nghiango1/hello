const one = 1;
const two = 2;

/**
 * Sum two number
 * @param {number} x number 1
 * @param {number} y number 2
 * @returns {number} result
 */
function add(x, y) {
  return x + y;
}

export { one, two, add };
