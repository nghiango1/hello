/**
 * concat two string
 * @returns {string} Result
 */
function conc(a, b) {
  return a + b;
}

/**
 * Sum two number
 * @param {number} a Number one
 * @param {number} b Number two
 * @returns {number} Result sum
 */
const add = function addTwoNumber(a, b) {
  return a + b;
};

const t = 1;

const s = conc('Hello', 'world');
console.log(add(t, s));
